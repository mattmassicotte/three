import C.stdio

def main(Int argc, **Char argv;Int)
  Int x
  Int y
  {Int, Int; Int} closure
  x = 42
  y = 43
  closure = do (Int a, Int b; Int; x) {
    x = a + b + y
    return a + b
  }
  printf("x = %d, value = %d\n", x, closure(1,2))
  printf("x = %d\n", x)
  return 0
end
