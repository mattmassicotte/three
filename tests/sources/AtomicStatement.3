def main(Int argc, **Char argv; Int)
    Int a
    Int b

    atomic
        a = 42
        atomic:strict
            a = 43
            b = 0
            abort if b > 0
        end
    else
        b = 1
    end

    return b
end
